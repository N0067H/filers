# FileRS 계획서

FileRS는 파일을 업로드하고 공유할 수 있는 웹 서비스다.

## 목적

본 프로젝트는 AI의 도움 없이 시스템 설계 및 구현 역량을 기르기 위한 프로젝트다. 처음부터 완벽한 설계를 목표로 하기보다, Iterative Refinement에 기반한 애자일 방식의 개발을 지향한다.

## MVP 범위

1. 파일 업로드
2. 파일 다운로드
3. 공유 링크 생성
4. 파일 메타데이터 저장
5. 파일 삭제
6. 업로드 크기 제한
7. 기본적인 권한 검사

## 고도화

- 파일 스토리지를 S3로 분리
  - 근데 `FileStorage` 추상화 만들어서 언제든지 `LocalStorage`/`S3Storage` 또는 다른 스토리지로 갈아끼울 수 있게 만들어야 할듯

- 디비에 주소는 남았지만 원본 파일이 없어졌을 때의 정합성을 고려해야 할듯


## 기술 선택

- **Rust + Axum:** 생산성만 고려한다면 Spring을 선택하는 것이 적절하다. 하지만 이번 프로젝트의 목적은 추상화 뒤에 숨겨진 문제를 직접 해결해 보는 데 있으므로 Rust를 선택했다.

- **PostgreSQL:** 파일 관리 서비스의 특성상 트랜잭션이 중요하다. 또한 Partial Index와 같은 PostgreSQL의 확장 기능도 활용 가치가 높다고 판단했다. 예를 들어, 삭제되지 않은 파일만을 대상으로 하는 인덱스를 구성할 수 있다.

## 초기 아키텍처

```text
Client
  |
  v
Axum Server
  |
  +── PostgreSQL
  |
  └── Local Filesystem
```

## 개발 시 주의사항

- AI는 사용하지 않는다.
- Rust 학습 프로젝트가 아니라 시스템 설계 프로젝트라는 점을 잊지 않는다.
- 오버엔지니어링은 지양한다.

